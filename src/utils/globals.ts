export const SHELL_PADDING = 100;
export const SHELL_HEIGHT = document.documentElement.clientHeight - (SHELL_PADDING * 2);
export const SHELL_WIDTH = document.documentElement.clientWidth - (SHELL_PADDING * 2);
export const TAB_PADDING = 30;
export const MOBILE_BREAKPOINT = 768;